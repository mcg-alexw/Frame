# Tasks — Lane Orchestrator — home screen for terminals as lanes

- T01 · Add `src/renderer/laneStatus.js` activity engine: own TERMINAL_OUTPUT_ID listener, quiet-timer + buffer classification into processing/waiting/idle, onChange subscription API, entry cleanup on terminal destroy.
- T02 · Pass Cmd/Ctrl+Escape through the terminal key handler in `terminalManager.js` and migrate persisted `viewMode: 'tabs'` sessions to the board/detail model.
- T03 · Add `laneBoard.js` + `styles/components/lane-board.css` and a `'board'` view mode in `multiTerminalUI.js`: card grid with name/project/status badge/last activity, click enters detail, auto-create on launch removed, empty state with CTA.
- T04 · Add board actions: "+ New Lane" card with shell selection, inline card rename, card close.
- T05 · Rework `terminalTabBar.js` into a mode-aware top bar: Lanes label (board), home button + status-badged lane switcher dropdown (detail), home + layout select (grid); delete tab strip rendering, tab rename, and tab context menu.
- T06 · Rebind commands in `index.js`: `lane.home` (CmdOrCtrl+Escape), `lane.toggleGrid` (board ↔ grid), Ctrl+Tab / Cmd+1-9 route through `enterLane()`.
- T07 · Make programmatic flows reveal their lane: `terminalCreateAndStart` / `restartTerminal` enter the new lane; `multiTerminalUI.sendCommand` enters the target lane from board and creates a lane when none exists.
- T08 · Remove remaining `'tabs'` view-mode remnants and `.terminal-tab` CSS, run `npm run structure`, verify spec success criteria 1–8 manually.
- T09 · Add `src/renderer/laneRail.js` context rail to the board (right side): Specs section (active specs with phase badge + task progress) and Tasks section (in_progress/pending with priority dots), items deep-linking to specsDashboard/tasksPanel/tasksDashboard, both sections and the whole rail collapsible with localStorage persistence and a slim reopen strip.
- T10 · Add `src/renderer/laneDetailRail.js`: collapsible lanes panel in the terminal detail view, priority-ordered (waiting → agent → busy → idle, then most recent activity), active lane highlighted, click switches lanes, collapse state persisted with slim reopen strip.
- T11 · Fold grid into the detail view as its layout: 1x1 default single terminal, larger layouts (1x2…3x3) render assignable cells — each cell has a lane-switch dropdown, empty cells are "New Lane" placeholders that create a terminal in place; standalone grid view mode, its toggle button and `lane.toggleGrid` command removed.
- T12 · Add a Frames tab to the top-bar left section in `terminalTabBar.js`: sibling of the Home tab carrying the Active Frames count, rendered only when ≥1 lane is open and always inserted right after Home; clicking it enters the active lane's detail view (falling back to the first lane) via new `multiTerminalUI.enterFrames()`. Rename the board tab label from "Mainframe" to "Home".
- T13 · Resolve the spec-detail A/B test in favour of the pinned section (matching tasks): add `src/renderer/specSection.js` so clicking a spec in the lane rail pins it as a top-bar section tab rendering the spec detail (stepper / next-action / spec·plan·tasks·outcome tabs / interactive task rows). Reroute `laneRail.js` and delete the now-retired `specDetailModal.js`. Section tabs are multi-instance: `taskSection.js`/`specSection.js` are instance factories and `multiTerminalUI.js` tracks `sections[]` + `activeSectionKey` (open several, switch via chips, close per-tab) — generalized the top-bar chip in `terminalTabBar.js` to carry a task **or** spec (spec uses a FileText icon).
- T14 · Retire the Tasks/Specs side panels now that Home's lane rail + section tabs cover them: route the top-bar Tasks icon (`btn-tasks-toggle`) to `tasksDashboard.toggle()` and the ⋯ More-menu Specs item to `specsDashboard.toggle()` in `terminalTabBar.js`; in `index.js` drop the `panel.toggleTasks` (Cmd+T) / `panel.toggleSpecs` (Cmd+Shift+S) commands and give the dashboards Cmd+Shift+D / Cmd+Shift+S. Keep `tasksPanel.js`/`specPanel.js` for their background roles only (spec watching feeds the rail; task loading feeds the shared cache).
- T15 · Make section tabs *navigable viewports* with an in-tab sibling rail so a user can review items without the Home→reselect→new-tab round-trip. Convert `taskSection.js`/`specSection.js` into viewport factories (mutable current item + `navigate()`); add `src/renderer/sectionRail.js` — a collapsible right-side sibling list (reuses lane-rail styling + localStorage hide state) where a normal click switches the viewport in place and Cmd/Ctrl-click opens a new viewport. `multiTerminalUI.openSection(type, itemRef, factory, {newTab})` reuses the existing viewport of the same type (no tab hell); the chip title always tracks the shown item. Lane-rail items gain the same Cmd/Ctrl-click affordance; section view becomes a two-column `section-layout` (content + rail) in `task-section.css`.
